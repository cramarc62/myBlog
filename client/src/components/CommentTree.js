import React from 'react'

export const CommentTree = (props) => {
    //let pixel=0;
    
    return (
        
           <>
                <span style={{marginLeft:props.pixel+"px"}}>{props.comment.body}</span>
           </> 
        //   

    )
}
